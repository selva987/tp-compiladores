b <= 1;
c <= "a ver si detecta esto como cadena o condicion";
a <= 35;

b <+ 2;

b <- 1;

ar[(b+1)] <= 50;

si((a igual 1) o (a igual 2)) {
	b <= 2;
}

mientras(b menor a) {
	b <= (b + 1);
}

para(i desde b hasta (a -1)) {
	b <= 2;
}

mostrar(b);
mostrar('hola');
pedir(a);

cargardesdearchivo(x,a);

suma <= 0;
i <= 0;

cada(el en x) {
	suma <+ el;
	i <+ 1;
}
mostrar("Promedio: ");
mostrar((suma / i));